<?xml version="1.0" encoding="utf-8"?>
<feed xmlns="http://www.w3.org/2005/Atom">

  <title><![CDATA[Category: Docker | My Octopress Blog]]></title>
  <link href="http://pricingblog.github.io/blog/categories/docker/atom.xml" rel="self"/>
  <link href="http://pricingblog.github.io/"/>
  <updated>2015-10-14T16:19:55+08:00</updated>
  <id>http://pricingblog.github.io/</id>
  <author>
    <name><![CDATA[Your Name]]></name>
    
  </author>
  <generator uri="http://octopress.org/">Octopress</generator>

  
  <entry>
    <title type="html"><![CDATA[Run Docker on Your Mac]]></title>
    <link href="http://pricingblog.github.io/blog/2015/10/14/run-docker-on-your-mac/"/>
    <updated>2015-10-14T15:32:03+08:00</updated>
    <id>http://pricingblog.github.io/blog/2015/10/14/run-docker-on-your-mac</id>
    <content type="html"><![CDATA[<p>这些年对Ops产生重大影响的工具应属<a href="http://www.docker.com" title="Docker Home Page">Docker</a>了。Docker基于Golang的实现横空出世，让应用部署产生了革命性的变革。Docker的存在让部署变得如此的容易，这也让microservices这种架构方式得到了良好的实施。</p>

<p>关于Docker, 它解决了三个问题:</p>

<ul>
<li>Build</li>
<li>Ship</li>
<li>Run</li>
</ul>


<p>本文讲讨论迈入Docker世界的第一步: <strong>如何将Docker装在你的Mac OS上</strong></p>

<h2>在Mac OS上安装Docker</h2>

<p>由于Docker底层基于linux, 在Mac OS上运行起来需要一个虚拟的linux环境，它需要若干工具支持:</p>

<ul>
<li>virtualbox: 虚拟机，用来跑linux</li>
<li>docker-machine: 用来管理虚拟机，之前用过的boot2docker已经合并到这个工具中</li>
<li>docker: docker本身</li>
<li>docker-compose(Mac OS only): 用来管理多个docker container, 如果你想做DB和App分离将会用到这个工具</li>
<li>Kitematic: 用来管理远程Docker Hub, 自己构建的Docker Image可以用它管理</li>
</ul>


<p>如果你想直接All in one安装，直接到这里下载安装即可安装好上述工具:  <br/>
<a href="https://www.docker.com/toolbox">https://www.docker.com/toolbox</a></p>

<p>个人倾向使用<a href="http://brew.sh/">Homebrew</a>安装上述工具:</p>

<ol>
<li>安装virtualbox</li>
<li>使用homebrew安装其他工具</li>
</ol>


<pre><code>brew install docker docker-machine docker-compose
brew cash install kitematic
</code></pre>

<h2>验证自己的环境</h2>

<h3>使用<code>docker-machine</code>创建<code>docker host</code></h3>

<pre><code>docker-machine create --driver virtualbox dev
</code></pre>

<p><code>dev</code>是当前<code>docker host</code>名字</p>

<h3>配置当前docker host环境</h3>

<p>使用<code>docker-machine env dev</code>查看刚刚创建的<code>dev</code>的信息:</p>

<pre><code>➜  devops  docker-machine env dev
export DOCKER_TLS_VERIFY="1"
export DOCKER_HOST="tcp://192.168.99.100:2376"
export DOCKER_CERT_PATH="/Users/lvjian/.docker/machine/machines/dev"
export DOCKER_MACHINE_NAME="dev"
# Run this command to configure your shell:
# eval "$(docker-machine env dev)"
</code></pre>

<p>将<code>dev</code>信息添加到环境变量中,这些环境变量将被<code>docker</code>使用.</p>

<pre><code>eval "$(docker-machine env dev)"
</code></pre>

<h3>运行docker run</h3>

<pre><code>docker run helloworld
</code></pre>

<p>此时<code>docker</code>会从<code>docker hub</code>上将<code>hello-world:last</code> image pull 下来，然后运行对应的<code>Dockerfile</code>. 如果运行成功将会看到如下信息:</p>

<pre><code>Hello from Docker.
This message shows that your installation appears to be working correctly.

To generate this message, Docker took the following steps:
 1. The Docker client contacted the Docker daemon.
 2. The Docker daemon pulled the "hello-world" image from the Docker Hub.
 3. The Docker daemon created a new container from that image which runs the
    executable that produces the output you are currently reading.
 4. The Docker daemon streamed that output to the Docker client, which sent it
    to your terminal.

To try something more ambitious, you can run an Ubuntu container with:
 $ docker run -it ubuntu bash

Share images, automate workflows, and more with a free Docker Hub account:
 https://hub.docker.com

For more examples and ideas, visit:
 https://docs.docker.com/userguide/
</code></pre>

<p>除此之外我们还可以运行其他image: <code>docker run -it ubuntu bash</code></p>

<p>到这里已经成功在mac os上安装了Docker.</p>

<h2>更多参考</h2>

<ul>
<li>install docker: <a href="http://docs.docker.com/mac/step_one/">http://docs.docker.com/mac/step_one/</a></li>
<li>docker hub: <a href="https://hub.docker.com">https://hub.docker.com</a></li>
<li>docker user guide:  <a href="https://docs.docker.com/userguide/">https://docs.docker.com/userguide/</a></li>
</ul>

]]></content>
  </entry>
  
</feed>
